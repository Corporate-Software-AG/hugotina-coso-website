new coso site with Tina CMS

For development:

```npm run dev```

For build:

```npm run build```

After developing at least run ```npm run dev```at least once. it will create the latest tina-lock.json file.
